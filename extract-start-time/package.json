{
  "name": "extract-start-time",
  "version": "1.0.0",
  "description": "uses dockerized ffmpeg to grab metadata from video file",
  "type": "module",
  "scripts": {
    "start": "node --experimental-specifier-resolution=node --loader ts-node/esm index.ts",
    "build": "tsc"
  },
  "author": "Tyler Bryant",
  "license": "UNLICENSED",
  "dependencies": {
    "zx": "^7.2.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}